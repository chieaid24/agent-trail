# Local verification stack (postgres + GitHub fixture). Baseline profile:
# the postgres image needs more freedom than restricted allows; the runner
# namespace is the one that enforces restricted.
apiVersion: v1
kind: Namespace
metadata:
  name: agent-trail-local
  labels:
    app.kubernetes.io/name: agent-trail
    pod-security.kubernetes.io/enforce: baseline
