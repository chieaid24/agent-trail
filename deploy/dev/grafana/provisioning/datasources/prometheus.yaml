# Dev Grafana: Prometheus datasource, provisioned so dashboards work on boot.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
