# Dev Grafana: Prometheus datasource, provisioned so dashboards work on boot.
# The uid is fixed; the provisioned dashboards and alert rules reference it.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
