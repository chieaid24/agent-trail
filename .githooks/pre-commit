#!/usr/bin/env bash
# Pre-commit hook: run the exact CI gate (format, lint, test, build, docs).
# Activate once per clone: git config core.hooksPath .githooks  (or make hooks)
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
exec bash scripts/gate.sh
